#server.port=80
#server.port.a=true
#server.port.b=true
server:
   port: 80
spring:
   datasource:
      url: jdbc:mysql://127.0.0.1:3306/C76-S3-Psq-blog
      username: root
      password: a
      driver-class-name: com.mysql.jdbc.Driver
   #静态资源映射
   resources:
      staticLocations:
         -classpath:/resources/
         -classpath:/static/
         -file:D:/other/javaExample
   #文件上传配置
   servlet:
      multipart:
         maxFileSize:10000000000
         
#mvc:
mybatis:
   type-aliases-package: com.yc.blog.bean
# view:
#   prefix: WEB-INF/page/
#   suffix: .jsp
   configuration:
      map-underscore-to-camel-case: true
#实体类的路径
# 日志配置
logging:
   level:
      com:
         yc:
            blog:
               dao: debug
               web: warn